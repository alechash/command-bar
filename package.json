{
    "name": "command-bar",
    "version": "1.0.0",
    "main": "main.js",
    "scripts": {
        "start": "electron .",
        "package": "electron-packager ./ 'Command Bar' --platform=darwin --arch=x64 --out=out --icon=./icon@4x.icns --overwrite"
    },
    "devDependencies": {
        "electron-packager": "^15.2.0",
        "electron-prebuilt": "^1.2.7",
        "electron-prebuilt-compile": "^1.7.9"
    },
    "dependencies": {
        "auto-launch": "^5.0.5"
    }
}